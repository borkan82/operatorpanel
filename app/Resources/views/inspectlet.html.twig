{% if panelType is not defined or panelType is null or panelType is empty or panelType == ""%}
    {% set panelType = "main" %}
{% endif %}

    {% if user is defined %}
        {% set kId = user.ouid %}
        {% set userIme = user.name %}
    {% endif %}

    {% block javascript %}
        <!-- Begin Inspectlet Embed Code -->
        <script type="text/javascript" id="inspectletjs">
            window.__insp = window.__insp || [];
            __insp.push(["resetSession"]);
            __insp.push(['wid', ***********************]);
            __insp.push(['identify', "{{ userIme }}"]);
            __insp.push(['tagSession', {state: "{{ state }}", panel: "{{ panelType }}", callId:"{{ kId }}"}]);
            (function() {
                function ldinsp(){if(typeof window.__inspld != "undefined") return; window.__inspld = 1; var insp = document.createElement('script');
                    insp.type = 'text/javascript'; insp.async = true; insp.id = "inspsync"; insp.src = ('https:' == document.location.protocol ? 'https' :
                                    'http') + '://cdn.inspectlet.com/inspectlet.js'; var x = document.getElementsByTagName('script')[0]; x.parentNode.insertBefore(insp,
                            x); };
                setTimeout(ldinsp, 500); document.readyState != "complete" ? (window.attachEvent ? window.attachEvent('onload', ldinsp) : window.addEventListener('load', ldinsp, false)) : ldinsp();
            })();
        </script>
        <!-- End Inspectlet Embed Code -->
    {% endblock %}